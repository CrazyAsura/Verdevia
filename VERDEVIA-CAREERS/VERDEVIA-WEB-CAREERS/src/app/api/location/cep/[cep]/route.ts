import { NextResponse } from 'next/server';
export async function GET(_:Request,{params}:{params:Promise<{cep:string}>}){const cep=(await params).cep.replace(/\D/g,'');if(cep.length!==8)return NextResponse.json({message:'CEP inválido'},{status:400});try{const r=await fetch(`https://brasilapi.com.br/api/cep/v2/${cep}`,{next:{revalidate:86400},signal:AbortSignal.timeout(8000)});if(!r.ok)return NextResponse.json({message:'CEP não encontrado'},{status:404});const d=await r.json();return NextResponse.json({zipCode:cep.replace(/(\d{5})(\d{3})/,'$1-$2'),street:d.street||'',district:d.neighborhood||'',city:d.city||'',state:d.state||''});}catch{return NextResponse.json({message:'Consulta de CEP indisponível'},{status:503});}}
