# =========================================================================
# ECOÁ - Nginx Gateway Configuration (Mobile Domain)
# =========================================================================

# HTTP Server — Redirect all traffic to HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name _;

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS Server — Reverse Proxy Gateway
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name _;

    # SSL Certificates (mounted from root certs directory)
    ssl_certificate /etc/nginx/certs/ecoa.crt;
    ssl_certificate_key /etc/nginx/certs/ecoa.key;

    # SSL Protocol Hardening
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384';
    
    # Security Headers
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

    # Proxy Buffer Tuning
    proxy_buffers 16 16k;
    proxy_buffer_size 32k;

    # Standard Proxy Headers
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Dynamic Resolver to allow startup resilience
    resolver 127.0.0.11 valid=5s;

    # Mobile API Gateway GraphQL Federation routing
    location /graphql {
        set $backend_upstream http://mobile-gateway:3333;
        proxy_pass $backend_upstream;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Mobile Backend REST API Routing (with rewrite)
    location /api/ {
        # Strip '/api' from path before sending to NestJS (e.g. /api/users -> /users)
        rewrite ^/api/(.*)$ /$1 break;
        
        set $backend_upstream http://mobile-backend:3334;
        proxy_pass $backend_upstream;
    }

    # Catch-all routes to Mobile API Gateway
    location / {
        set $backend_upstream http://mobile-gateway:3333;
        proxy_pass $backend_upstream;
    }
}
